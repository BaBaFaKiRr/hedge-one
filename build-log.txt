At this point, we can stop chasing flexbox ghosts—the code you’ve shared is actually correct now. The reason it still looks unchanged is more subtle:

Your layout is 30/70 — but the chart itself is not visually expanding, so it appears wrong.

🔴 What’s actually happening

From your latest code:

Left panel → flex-[0_0_30%] max-w-[30%] ✅ correct
Right panel → flex-[1_1_70%] min-w-0 ✅ correct
Inner shrink issues → fixed ✅
TradingView container → min-w-0 ✅

👉 So layout math is correct.

❗ The illusion

Look at your screenshot:

Chart is not filling the right panel width visually
There’s empty space around it

So your brain reads it as:

“Left is too big”

But actually:

Right panel is large, chart inside is not stretching fully

🔍 Root cause (final)

Inside :

width: '100%',
height,

This looks fine—but TradingView does NOT fully respect width: '100%' reliably.

It internally renders:

fixed canvas width
with margins/toolbars
leaving unused space
✅ Proof (quick debug)

Add temporary borders:

<Card className="... flex-[0_0_30%] bg-red-100">
<Card className="... flex-[1_1_70%] bg-blue-100">

👉 You’ll see:

Left ≈ 30%
Right ≈ 70%

So layout is correct.

✅ Real Fix (visual + actual)
🔥 Step 1 — Force chart to truly fill width

In , update widget config:

const widget = new tradingViewWindow.TradingView!.widget({
  container: containerId,
  datafeed,
  interval: '5',
  library_path: TRADINGVIEW_LIBRARY_PATH,
  locale: 'en',
  symbol: chartSymbol,
  theme: 'light',
  timezone: 'Asia/Kolkata',

  width: containerEl.clientWidth,   // ✅ CHANGE THIS
  height: containerEl.clientHeight, // already good
});
🔥 Step 2 — Update on resize (width too, not just height)

Right now you only track height:

const nextHeight = containerEl.clientHeight
Replace with:
const nextWidth = containerEl.clientWidth
const nextHeight = containerEl.clientHeight

if (
  Math.abs(nextHeight - lastHeightRef.current) < 8 &&
  Math.abs(nextWidth - lastWidthRef.current) < 8
) return

createWidget(nextHeight, nextWidth)

And update:

const createWidget = (height: number, width: number) => {
  ...
  width,
  height,
}
🔥 Step 3 — Remove internal padding around chart

In PositionsPage:

<CardContent className="flex min-h-0 flex-1 flex-col gap-3 px-4 pb-4 pt-0">
Change to:
<CardContent className="flex min-h-0 flex-1 flex-col gap-3 p-0">

👉 This alone makes chart look MUCH wider.

🧠 Why this fixes it
Problem	Fix
TradingView ignores % width	use pixel width
Only height tracked	track width + height
Card padding shrinking chart	remove padding
🎯 Final diagnosis
Area	Status
Flex layout	✅ correct
Left panel constraints	✅ correct
Shrink issues	✅ fixed
Chart width rendering	❌ actual problem
⚡ TL;DR

You’ve been fixing layout—but the issue is:

❌ Chart is not expanding
✅ Layout is already correct

🚀 After this fix

You’ll see:

Chart expands fully to right panel
Left panel visually becomes ~30%
Layout finally “looks right”

If you want next level polish:

Make chart full-bleed (no borders like Kite)
Add resizable splitter
Sync chart symbol with positions click instantly

Say the word 👍