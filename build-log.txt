You’re actually very close—the core issue isn’t your data logic, it’s your layout constraints + height management + breakpoint strategy. Right now your UI looks stacked / uneven because of a few subtle but critical flexbox mistakes.

I’ll walk you through the exact problems in your code and how to fix them cleanly.

🔴 Root Problems
1. Your layout only becomes 2-column after 500px
min-[500px]:flex-row

That means:

Below 500px → stacked (fine)
But also: your container sizing depends heavily on this breakpoint

👉 If your parent container isn’t giving proper height, the chart collapses.

2. You are fighting height with 100dvh hacks
min-[500px]:h-[calc(100dvh-9rem)]
min-[500px]:max-h-[calc(100dvh-9rem)]

This is the main reason your layout feels broken.

Problems:

100dvh is unstable inside nested layouts
If parent doesn’t have fixed height → children collapse
Causes scroll issues + chart compression
3. Your left column is width-constrained too aggressively
min-[500px]:w-[280px]
min-[500px]:max-w-[min(360px,34%)]

This leads to:

inconsistent width scaling
weird proportion when screen grows
4. TradingView chart needs guaranteed height chain

You did:

<div className="flex min-h-[420px] ... flex-1">

But if any parent above lacks min-h-0 or proper flex, it collapses.

✅ What You Actually Want (Clean Architecture)

You want a stable split layout:

-----------------------------------
| Left (list) | Right (chart)     |
| scrollable  | full height chart |
-----------------------------------
✅ Correct Fix (Minimal but Powerful)
🔧 Step 1 — Simplify the container (CRITICAL)

Replace this entire wrapper:

<div className="flex min-h-0 w-full flex-1 flex-col gap-4 min-[500px]:h-[calc(100dvh-9rem)] ...">

👉 WITH THIS:

<div className="flex flex-1 min-h-0 w-full flex-col gap-4 md:flex-row">

✔ No 100dvh
✔ Clean responsive breakpoint (md)
✔ Stable flex behavior

🔧 Step 2 — Fix Left Column (List)

Replace width logic with:

<Card className="flex flex-col min-h-0 w-full md:w-[320px] md:min-w-[280px] md:max-w-[360px] overflow-hidden">

✔ Fixed usable sidebar
✔ Predictable behavior

🔧 Step 3 — Fix Right Column (Chart)
<Card className="flex flex-col flex-1 min-h-0 overflow-hidden">
🔧 Step 4 — Fix Chart Container (MOST IMPORTANT)

Replace:

<div className="flex min-h-[420px] w-full min-w-0 flex-1 flex-col">

👉 WITH:

<div className="flex flex-1 min-h-0 w-full">
  <TradingViewPositionChart ... />
</div>
🔧 Step 5 — Ensure TradingView Component Fills

Inside TradingViewPositionChart:

<div className="h-full w-full">

If this is missing → chart will shrink.

🧠 Why Your Current UI Looks Wrong

From your screenshot:

Chart is compressed horizontally
Layout feels like stack inside stack
Spacing inconsistent

👉 That happens when:

Parent height is undefined
Child uses min-h instead of flex-1
dvh conflicts with flexbox
✅ Final Clean Layout Model

Here’s the mental model you should follow:

RULES:
Never mix vh/dvh with flex layouts unless necessary

Every flex parent must have:

min-h-0
Scroll should exist in only one place (your left list ✔️)

Chart must always live inside:

flex-1 + min-h-0
⚡ Bonus Improvement (You should do this)
Sticky header inside left panel
<CardHeader className="sticky top-0 bg-white z-10 border-b">
Smooth UX Upgrade

Highlight selected trade → already good
But also scroll into view:

ref?.scrollIntoView({ block: "nearest" })
🧩 If You Want Next Step

I can:

Refactor this into a production-grade trading dashboard layout
Add:
real-time pnl updates (socket-based)
multi-position comparison
PnL curve graph per trade
order execution overlay on chart (pro-level feature)

Just tell me 👍